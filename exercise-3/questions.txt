Questions, assignment 3

1. Absolute positioning is relative to the closest parent element
which is not statically positioned, while fixed positioning is always
relative to the viewport.

2. We can use the :nth-child() pseudo-class for that; * > :nth-child(3)
would select the third direct child of any element in a document.